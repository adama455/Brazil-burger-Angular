import { MouvementDirective } from './mouvement.directive';

describe('MouvementDirective', () => {
  it('should create an instance', () => {
    const directive = new MouvementDirective();
    expect(directive).toBeTruthy();
  });
});
